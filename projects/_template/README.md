# Blank project template

Create a project through `pixi run pipeline init-project --project-id <id>`.
The copied configuration is deliberately unconfirmed and cannot run until the
user has answered the provenance, design, contrast, upstream and module
questions. `formats: [pdf]` is the default; add `png` only after an explicit
request.
