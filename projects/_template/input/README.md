# Input directory

Do not edit an imported count matrix. Use `pixi run import-counts -- ...` so the
file is copied here and a SHA256 provenance manifest is written. FASTQ files,
STAR indices and other large upstream inputs stay outside Git.
